# data empty_funcname := . # parse error

# val empty_funcname: type [max_card 0]. # max_card 0 is not taken into account

val empty: type.

goal ~ (forall (e: empty). ((true = false) asserting false)).

# axiom forall (fn: empty_funcname). false.
